[build-system]
requires = ["setuptools>=69.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sflow"
version = "0.2.1"
description = "A Python CLI tool designed to automate and manage benchmarking workflows on multiple backends."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [
    {name = "NVIDIA Corporation"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: System :: Distributed Computing",
]

dependencies = [
    "jinja2>=3.1.6",
    "mermaid-py>=0.8.0",
    "parse==1.16.0",
    "pydantic>=2.11.10",
    "pyyaml>=6.0.3",
    "rich>=13.7.0",
    "textual>=8.2.7",
    "typer>=0.19.2",
]

[project.urls]
Homepage = "https://github.com/NVIDIA/nv-sflow"
Documentation = "https://nvidia.github.io/nv-sflow/"
Repository = "https://github.com/NVIDIA/nv-sflow"
"Bug Tracker" = "https://github.com/NVIDIA/nv-sflow/issues"

[project.scripts]
sflow = "sflow.cli:app"

[project.optional-dependencies]
dev = [
    "ipython>=8.37.0",
    "matplotlib>=3.8.0",
    "moto>=5.0.0",
    "pytest>=8.4.2",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=7.0.0",
    "pytest-subprocess>=1.5.3",
]
s3 = [
    "boto3>=1.34.0",
]
# Optional PNG timelines for the `monitor:` feature. CSV monitor reports and the
# terminal sflow_monitor.log overview work without this; only PNG rendering needs it.
monitor = [
    "matplotlib>=3.8.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"sflow.samples" = ["*.yaml", "*.csv"]
"sflow.samples.inference_x_v2" = ["*.yaml", "*.csv"]
"sflow.samples.inference_x_v2.trtllm" = ["*.yaml"]
"sflow.samples.inference_x_v2.vllm" = ["*.yaml"]
"sflow.samples.inference_x_v2.sglang" = ["*.yaml"]
"sflow.samples.inference_x_v2.composed_recipes" = ["*.yaml"]
"sflow.skills" = ["*.md", "**/*.md", "**/*.py"]
"sflow.monitoring" = ["*.py"]

[dependency-groups]
dev = [
    "ipython>=8.37.0",
    "matplotlib>=3.8.0",
    "moto>=5.0.0",
    "pytest>=8.4.2",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=7.0.0",
    "pytest-subprocess>=1.5.3",
]


[tool.ruff.lint]
exclude = [
    "tests/*",
    "tests/**/*",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "tests/*",
    "*/tests/*",
    "*/__init__.py",
    "*/site-packages/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]
show_missing = true
skip_covered = false

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--cov=src",
    "--cov-report=term-missing",
    "--cov-report=xml",
]


