[build-system]
requires = ["setuptools>=69.0.0", "setuptools-scm>=8", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sflow"
dynamic = ["version"]
description = "A Python CLI tool designed to automate and manage benchmarking workflows on multiple backends."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [
    {name = "NVIDIA Corporation"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: System :: Distributed Computing",
]

dependencies = [
    "jinja2>=3.1.6",
    "mermaid-py>=0.8.0",
    # Range, not a pin: an exact pin here makes sflow uninstallable alongside any
    # tool that needs a newer parse (e.g. nv-benchmark-hpcdl>=4.0.1 wants >=1.21).
    # 1.16 is the floor sflow is verified against; <2 guards the next major.
    "parse>=1.16,<2",
    "pydantic>=2.11.10",
    "pyyaml>=6.0.3",
    "rich>=13.7.0",
    "textual>=8.2.7",
    "typer>=0.19.2",
]

[project.urls]
Homepage = "https://github.com/NVIDIA/nv-sflow"
Documentation = "https://nvidia.github.io/nv-sflow/"
Repository = "https://github.com/NVIDIA/nv-sflow"
"Bug Tracker" = "https://github.com/NVIDIA/nv-sflow/issues"

[project.scripts]
sflow = "sflow.cli:app"

[project.optional-dependencies]
dev = [
    "ipython>=8.37.0",
    "matplotlib>=3.8.0",
    "moto>=5.0.0",
    "pytest>=8.4.2",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=7.0.0",
    "pytest-subprocess>=1.5.3",
    "pytest-xdist>=3.8.0",
]
s3 = [
    "boto3>=1.34.0",
]
# Optional PNG timelines for the `monitor:` feature. CSV monitor reports and the
# terminal sflow_monitor.log overview work without this; only PNG rendering needs it.
monitor = [
    "matplotlib>=3.8.0",
]

[tool.setuptools_scm]
version_file = "src/sflow/_version.py"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"sflow.samples" = ["**/*.yaml", "**/*.csv", "**/*.md"]
# gpu_placement.sh is sourced by every Slurm GPU step; without it the operator
# skips GPU placement entirely, so it must ship in the wheel.
"sflow.plugins.operators" = ["*.sh"]
"sflow.skills" = ["*.md", "**/*.md", "**/*.py"]
"sflow.monitoring" = ["*.py"]

[dependency-groups]
dev = [
    "ipython>=8.37.0",
    "matplotlib>=3.8.0",
    "moto>=5.0.0",
    "pytest>=8.4.2",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=7.0.0",
    "pytest-subprocess>=1.5.3",
    "pytest-xdist>=3.8.0",
]


[tool.ruff.lint]
exclude = [
    "tests/*",
    "tests/**/*",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "tests/*",
    "*/tests/*",
    "*/__init__.py",
    "*/site-packages/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]
show_missing = true
skip_covered = false

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--cov=src",
    "--cov-report=term-missing",
    "--cov-report=xml",
]


